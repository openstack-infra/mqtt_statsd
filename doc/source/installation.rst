============
Installation
============

At the command line::

    $ pip install mqtt_statsd

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mqtt_statsd
    $ pip install mqtt_statsd
